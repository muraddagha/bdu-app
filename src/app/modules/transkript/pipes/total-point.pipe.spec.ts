import { TotalPointPipe } from './total-point.pipe';

describe('TotalPointPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalPointPipe();
    expect(pipe).toBeTruthy();
  });
});
