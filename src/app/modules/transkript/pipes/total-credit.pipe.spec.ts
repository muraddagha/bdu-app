import { TotalCreditPipe } from './total-credit.pipe';

describe('TotalCreditPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalCreditPipe();
    expect(pipe).toBeTruthy();
  });
});
